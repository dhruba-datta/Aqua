[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — every unmatched path serves index.html so React Router can handle it
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
